# Release Gates for Oracle Cloud Marketplace

The generated package is complete for internal review and evaluation packaging, but it is not authorized for external marketplace publication.

1. Complete OCI partner and Marketplace Publisher onboarding.
2. Choose lead generation or a supported OCI application package type.
3. Replace SNAPSHOT artifacts and validate OCI Data Flow compatibility.
4. Build, scan, and upload the container artifact to OCI Publisher when using a container listing.
5. Complete paid-listing eligibility, terms, support, and regional availability review.

The `submissionReady` field in `manifest.json` must remain `false` until all gates are evidenced, the package is regenerated from immutable artifacts, and Yanbor LLC authorizes publication.
