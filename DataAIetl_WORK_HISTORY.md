# DataAI ETL Work History

This file is a permanent local work history for `C:\Projects\DataAI.Etl`.

## Update Rule

- Update this file every day that DataAI ETL work is performed.
- Keep entries concise but specific enough to restore project context after
  chat compaction.
- Do not commit this file unless explicitly requested.

## Major Work History

- Created the standalone Maven multi-module DataAI Spark SDK for Java 17,
  Apache Spark 3.5.0, and Scala 2.12.
- Added public API, quality, core, test-kit, functions, and shaded CLI modules.
- Added normalization, deterministic record keys, field profiling, clean and
  rejected record routing, quality findings, summaries, and quality gates.
- Added declarative required, unique, minimum, maximum, between, accepted-set,
  date-format, length, equality, and regex rules.
- Added grouped analytics, pivot, variance, ranking, correlation, linear
  regression, time summaries, rolling analysis, outliers, anomaly scoring,
  drift, ABC Pareto, cohorts, funnels, and KPIs.
- Added demand, pricing, elasticity, basket, segments, churn, risk, inventory,
  profit, and scenario market functions.
- Added map-readiness classification, matrix balancing, automatic quality
  diagnostics, data dictionary, chart recommendations, alerts, and local
  deterministic narratives.
- Added Oracle AIDP configuration examples, customer installation guidance,
  and the complete embeddable function catalog.
- Verified the complete Maven reactor and real local Spark execution tests.

## 2026-08-06

- Added `DataAIetl_WORK_RULES.md` as the DataAI ETL equivalent of the
  DataAISandbox work rules.
- Added ETL-specific safeguards for embedded-library boundaries, Spark runtime
  compatibility, module ownership, DataFrame execution, testing, distribution,
  documentation, licensing, credentials, and customer data.
- Created this separate daily work-history file as required by the work rules.
- Added `LICENSE.md` using the approved PolyForm Free Trial 1.0.0 plus separate
  commercial-license distribution model.
- Added the README licensing summary: evaluation is limited to less than 32
  consecutive calendar days, while production, continued use, and
  redistribution require written commercial terms.
- Added `COMMERCIAL_LICENSE_TEMPLATE.md` with sample internal-production,
  source-code, OEM/redistribution, managed-service, license-metric, support,
  privacy, warranty, indemnity, liability, termination, audit, order-form, and
  distribution-checklist terms for counsel review.
- Added explicit **AS IS** and no-obligation provisions to the evaluation and
  sample commercial licenses. The commercial provision preserves only duties
  expressly accepted in a signed agreement or applicable order form.
- Replaced the sample commercial performance warranty and correction/refund
  remedy with customer evaluation, acceptance, and suitability-responsibility
  terms.
- Added an optional, separately signed extended evaluation path of up to 60
  consecutive calendar days without changing the standard PolyForm trial.
- Removed the extended 60-day evaluation path and its order-form template after
  product-policy reconsideration. The only evaluation period is again the
  standard PolyForm period of less than 32 consecutive calendar days.
- Added the optional `Tableau/` Maven module with stable output names,
  `TableauOutputs`, `TableauOutputBundle`, and real local Spark execution tests.
- Added Tableau-friendly run and dashboard DataFrames while preserving
  customer-controlled writes and keeping core DataAI modules independent of
  Tableau.
- Added a complete Tableau customer bundle: fictional CSV data, generated
  Hyper extract, packaged Accelerator workbook, schema mapping, Spark SQL and
  Databricks examples, Exchange listing copy, icon, design-preview screenshots,
  deterministic asset generation, offline validation, and checksums.
- Documented Tableau installation, test-drive, connection, distribution,
  marketplace, licensing, and publication-QA boundaries across the repository.
- Made `dataai-spark-tableau` depend transitively on the complete
  `dataai-spark-functions` artifact, added universal function-result metadata,
  and added dedicated matrix-balancing conversion with convergence metadata,
  example code, documentation, and a real Spark execution test.
- Verified the full eight-module Java 17 / Spark 3.5 Maven reactor, including
  three Tableau tests and real matrix balancing, with `BUILD SUCCESS`; verified
  the adapter JAR contains no bundled Spark or Hadoop classes.
- Added the optional `IRIS/` Maven module with safe JDBC options, table/query/
  partitioned reads, caller-controlled writers, stable IRIS output names,
  pipeline output bundles, and universal function-result metadata.
- Made `dataai-spark-iris` depend transitively on the complete DataAI functions
  artifact and added dedicated matrix-balancing conversion with convergence,
  iteration, maximum-error, run, version, and platform metadata.
- Added fictional order and matrix data, Java/SQL/spark-submit examples, IRIS
  schema mappings, Open Exchange listing copy, optional public IPM bootstrap,
  commercial/evaluation license copies, third-party notices, marketplace icon,
  design-preview screenshots, controlled evaluation ZIP, and checksums.
- Added deterministic IRIS asset generation and offline package validation;
  documented the boundary between the public free IPM bootstrap and privately
  fulfilled commercial Spark artifacts.
- Verified the full nine-module Java 17 / Spark 3.5 Maven reactor, including
  four IRIS adapter tests and real matrix balancing, with `BUILD SUCCESS`.
- Added marketplace upload maps and produced root-level `DataAIETLIRIS.zip`
  and `DataAIETLTableau.zip` submission kits containing the respective listing
  copy, logos, screenshots, documentation, evaluation artifacts, licenses,
  checksums, source, tests, scripts, and verified adapter JARs.

## 2026-08-07

- Identified Yanbor LLC as the legal Licensor and provider of the DataAI
  product across the evaluation license, commercial agreement template,
  signature blocks, order form, notices, ownership and obligation clauses,
  package documentation, manifests, and marketplace listings.
- Preserved DataAI and DataAI ETL as product names rather than legal entities,
  then regenerated all license copies, checksums, evaluation artifacts, and
  marketplace submission ZIPs.
- Added the approved proprietary/source-available/open-source-technology
  classification statement to every DataAI ETL `README.md` file and
  regenerated the IRIS and Tableau distributions.
- Created `SparkETL/` as a dedicated Spark marketplace-review and controlled
  evaluation package with Maven artifacts, source/Javadoc JARs, POMs, source,
  documentation, examples, listing copy, DataAI-branded media, third-party
  notices, deterministic generation, checksums, and offline validation.
- Documented that private Maven fulfillment is the primary commercial library
  channel, while Maven Central and cloud marketplaces require their respective
  immutable-release, namespace, metadata, signing, delivery-model, and partner
  approval gates.
- Verified the complete nine-module Java 17 / Spark 3.5 reactor with
  `BUILD SUCCESS`, generated 15 customer-facing binary/source/Javadoc JARs,
  validated marketplace media and archive structure, confirmed Spark/Hadoop
  classes are not bundled, and added a final ZIP sidecar checksum.
- Adopted the repository-root `SparkETL.zip` distribution location, moved its
  SHA-256 sidecar to the same location, and aligned the deterministic generator,
  offline validator, and package documentation with that layout.
- Created `Databricks/` as a Databricks Marketplace provider and controlled
  evaluation kit with personalized-listing guidance, Unity Catalog delivery
  templates, importable notebooks, fictional quality and matrix data, runtime
  compatibility and security documentation, listing copy, provider checklist,
  generated media, deterministic archives, and offline validation.
- Kept the Databricks integration library-only and customer-controlled: DataAI
  runs inside customer compute, all writes default to disabled, and production
  artifacts remain privately fulfilled after commercial licensing.
- Verified the complete nine-module Java 17 / Spark 3.5 Maven reactor with
  `BUILD SUCCESS`, copied the freshly built API, quality, core, functions, and
  CLI JARs, confirmed that Spark and Hadoop classes are not bundled, and
  validated notebook syntax, fictional data, media, checksums, and archives.
- Produced reproducible repository-root `DataAIETLDatabricks.zip` and SHA-256
  sidecar plus the customer-facing Databricks evaluation ZIP; repeated
  generation produced the same provider-archive digest.
- Created separate `AWS/`, `Microsoft/`, `Oracle/`, `Google/`, and `Maven/`
  marketplace review folders with provider instructions, listing copy,
  checklists, installation and release-gate documentation, licenses, complete
  function catalogs, verified JAR payloads, DataAI media, evaluation ZIPs,
  deterministic generation, checksums, and offline validation.
- Added channel-specific customer-run container candidates for AWS EMR,
  Microsoft Azure offer review, OCI delivery, and Google Cloud delivery without
  adding a hosted DataAI service. Documented raw-JAR, CNAB, public registry,
  partner approval, immutable release, security, runtime, and pricing gates.
- Added a Maven Central pre-publication package with 15 binary/source/Javadoc
  JARs, POMs, Maven repository layout, Central plugin and settings templates,
  and an explicit upload block for the current SNAPSHOT, unsigned, unverified-
  namespace artifacts.
- Produced and validated root-level `DataAIETLAWS.zip`,
  `DataAIETLMicrosoft.zip`, `DataAIETLOracle.zip`, `DataAIETLGoogle.zip`, and
  `DataAIETLMaven.zip` with matching SHA-256 sidecars.
- Created `NuGet/` with the `Yanbor.DataAI.Etl.Spark` .NET 8 integration helper
  and `Yanbor.DataAI.Etl.Spark.Runtime` transitive runtime package. Added typed
  quality-job JSON rules, Spark runtime discovery, safe `spark-submit` command
  construction, a 43-entry function catalog including matrix balancing,
  customer installation documentation, provider gates, and private-feed
  distribution guidance without adding a hosted service or remote check.
- Built both `0.1.0-evaluation.1` packages, verified their embedded license,
  notices, icon, assemblies, four unshaded DataAI library JARs, shaded CLI JAR,
  and MSBuild copy targets, then passed source-level validation and a clean
  local-feed restore/build/run test with zero build warnings or errors.
- Re-ran the complete nine-module Java 17 / Spark 3.5 Maven reactor after the
  NuGet work with `BUILD SUCCESS`, confirmed the packaged JAR entry contents
  match the freshly built artifacts, and verified that none of the five JARs
  bundles Spark or Hadoop classes.

## 2026-08-09

- Created `Alteryx/` as a source-available evaluation and Marketplace review
  kit for a customer-embedded DataAI ETL custom tool, without adding a hosted
  service, telemetry, remote license check, or customer-data transfer to Yanbor
  LLC.
- Added the **DataAI ETL Quality** Alteryx AMP tool for customer-selected
  `spark-submit`, Spark master/deploy mode, source and output catalog tables,
  normalization, record keys, JSON rules, minimum quality score, advanced
  Spark arguments, timeout, evaluation-license acceptance, and a generic
  status output that does not return customer rows or raw Spark logs.
- Restricted process execution to Spark submit executables, fixed the DataAI
  Java main class, passed an argument vector with `shell=False`, used a
  transient local JSON configuration that is deleted after execution, and
  documented that each configured Delta output table is overwritten.
- Targeted the generated evaluation YXI to Alteryx Designer 2026.1, AMP,
  embedded Python 3.13.11, AYX Plugin CLI 1.3.2, and AYX Python SDK 2.5.3;
  bundled the official Python 3.13 SDK dependency set while continuing to
  treat Spark and Hadoop as customer-provided dependencies.
- Added installation and usage instructions, runtime compatibility, security
  and data-handling documentation, complete DataAI function catalog including
  matrix balancing, fictional sample data and rules, third-party notices,
  Marketplace listing copy, Creator Portal submission steps, provider release
  gates, and clearly labeled design-preview media.
- Produced the installable YXI, customer evaluation archive, SHA-256 manifests,
  and root-level `DataAIETLAlteryx.zip` submission kit with a matching sidecar;
  retained `submissionReady: false` and external Designer/Server validation
  flags as false until the candidate is run in Alteryx and SNAPSHOT JARs are
  replaced with immutable releases.
- Passed seven offline adapter tests covering executable allow-listing, fixed
  main-class enforcement, evaluation acceptance, Java CLI JSON mapping,
  RuleSpec schema rejection, `shell=False`, timeout propagation, and
  temporary-file cleanup; passed the complete offline archive, license,
  checksum, source, UI, and CRLF validator.
- Re-ran the complete nine-module Java 17 / Spark 3.5 Maven reactor with Maven
  3.9.9 using the project-local offline repository and obtained
  `BUILD SUCCESS`; four Spark-backed suites ran 12 tests with zero failures,
  errors, or skips, including core, functions, Tableau, and IRIS tests.
- Audited the ten repository-root ETL ZIPs and both NuGet packages, reviewed
  their manifests, release states, customer artifacts, checksums, existing
  publication instructions, and installation guides, and rechecked the target
  marketplace routes against current official provider documentation.
- Re-ran every available Spark, Databricks, AWS, Microsoft, Oracle, Google,
  Maven, IRIS, Tableau, Alteryx, and NuGet offline validator. All packages now
  pass their offline structural, manifest, content, checksum, and applicable
  adapter-test checks, while every public production submission remains blocked
  by the documented immutable-release and external provider/runtime gates.
- Refreshed IRIS and Tableau generated assets after the Maven-built adapter
  JARs changed, corrected their internal checksum manifests, rebuilt
  `DataAIETLIRIS.zip` and `DataAIETLTableau.zip`, and added matching root-level
  SHA-256 sidecars.
- Added `MARKETPLACE_PUBLISHING_GUIDE.md`, consolidating the readiness decision,
  actual upload artifact, destination, provider onboarding, release gates, and
  step-by-step publication procedure for generic Spark, Databricks, AWS,
  Microsoft, Oracle, Google, Maven Central, NuGet, Tableau, InterSystems Open
  Exchange, and Alteryx.
- Added `CUSTOMER_INSTALLATION_GUIDE.md`, consolidating checksum, licensing,
  prerequisites, acquisition, installation, first-run, validation, production
  boundary, and troubleshooting instructions for every DataAI ETL distribution
  format.
- Added `PRODUCTION_RELEASE_GUIDE.md`, defining the shared immutable-release
  pipeline and the exact deliverables, rebuild commands, external-platform
  tests, publication gates, and sign-off evidence required to turn each current
  Spark, Databricks, cloud, Maven, NuGet, Tableau, IRIS, and Alteryx evaluation
  kit into a production release.
- Completed the final repository-local Java 17 / Maven 3.9.9 offline
  `mvn clean verify` reactor with `BUILD SUCCESS`; the four Spark-backed suites
  ran 12 tests with zero failures, errors, or skips. Documented that the normal
  lifecycle does not regenerate source/Javadoc companion JARs and that a
  production release profile must make those artifacts reproducible.
- Regenerated SparkETL, Databricks, AWS, Microsoft, Oracle, Google, Maven,
  Tableau, IRIS, Alteryx, and NuGet deliverables from the final Maven-built
  binaries, including a rebuilt Alteryx YXI and clean NuGet consumer restore.
- Passed the complete post-regeneration validator matrix, opened and tested all
  ten root ZIPs, opened both NuGet packages, verified all ten root SHA-256
  sidecars, verified CRLF/UTF-8 for the consolidated guides and history, and
  confirmed that all three guides cover every distribution channel.
- Created the isolated `C:\Projects\DataAIETLproduction` workspace without
  converting or overwriting the evaluation tree, and standardized executable
  packaging, manifests, runtime reporting, examples, and customer archive names
  on immutable version `1.0.0` with production-candidate status.
- Added the Maven `release-artifacts` profile so binary, source, and Javadoc
  JARs are recreated by the release build; ran the nine-module Java 17 / Spark
  3.5.0 reactor with 12 tests passing and zero failures, errors, or skips.
- Rebuilt and offline-validated the Spark, Databricks, AWS, Microsoft, Oracle,
  Google, Maven, Tableau, IRIS, Alteryx, and NuGet production-candidate
  products from one canonical JAR set. Alteryx passed seven adapter/security
  tests; both NuGet packages passed a clean local-feed consumer build and run.
- Created versioned IRIS and Tableau provider archives and assembled 13 final
  deliverables under `C:\Projects\DataAIETLproduction\artifacts`, including ten
  provider ZIPs, two NuGet packages, and the direct Alteryx YXI.
- Added deterministic release assembly and validation scripts, SHA-256 and
  machine-readable artifact inventories, a bundled-component inventory, and
  `PRODUCTION_RELEASE_REPORT.md`. Final validation opened every archive,
  verified all hashes, rejected stale evaluation/SNAPSHOT artifact names,
  confirmed canonical JAR equality, and confirmed that Spark/Hadoop classes
  are not bundled.
- Retained `submissionReady: false` and external-validation flags as false.
  Documented that cloud images/CNABs, exact external platform execution,
  marketplace onboarding/certification, signing, formal SBOM/security review,
  legal approval, and Yanbor release authorization remain external gates.
- Corrected production documentation terminology after owner review: removed
  trial-marketing language from production guides, listings, generators,
  validators, samples, and customer instructions; used production validation
  and acceptance-testing language instead.
- Regenerated and revalidated every Spark-backed provider kit, Tableau and IRIS
  archive, Alteryx YXI/provider package, and both NuGet packages, then rebuilt
  all 13 final artifacts. Extended the release-set validator to reject the
  removed terminology inside final archives as well as stale development
  versions.
- Converted `C:\Projects\DataAIETLproduction` to a production-only commercial
  distribution model. Replaced the root and ten product license copies with a
  canonical production distribution notice, replaced the commercial agreement
  template with a production-only sample, removed copied non-production
  documentation and internal work-rule/history files, and retained the
  canonical operational records only in `C:\Projects\DataAI.Etl`.
- Renamed the Databricks job template and provider volume for production use,
  changed Alteryx to the **Designer 2026.1 Production** edition and commercial
  offer, renamed the Alteryx UI entitlement field to
  `commercialRightsConfirmed`, and converted Spark, Databricks, Tableau, IRIS,
  cloud, Maven, and NuGet customer-facing terms to commercial fulfillment.
- Rebuilt and passed every product generator and offline validator, seven
  Alteryx adapter/security tests, two NuGet package builds with a clean
  zero-warning consumer build, and the 13-artifact release-set validator.
  A recursive audit scanned 37 archive files, 53 nested archive layers, and
  1,573 first-party members without finding obsolete customer-trial wording or
  stale DataAI prerelease markers; all ten product license/template hashes
  match their canonical production files.
- Corrected `DataAIETLmarkets.html` license delivery by replacing shared
  Markdown targets with 22 browser-safe, product-specific HTML license pages:
  one evaluation and one production page for each of the 11 listed products.
  Each page is generated from the `LICENSE.md` embedded in its exact download
  package. Verified all displayed license bodies against their archives and
  confirmed all 22 URLs return HTTP 200 through the local IIS application.
- Added `InstallationGuide/` to the DataAI Sandbox site with 11 evaluation
  and 11 production HTML installation guides plus an index. Rendered the
  product-specific `INSTALLATION_AND_USAGE.md` documents; for IRIS and Tableau,
  rendered the exact package `README.md` files that contain their installation
  and usage documentation. Added a unique **See more...** guide link to each of
  the 22 market-table instruction cells.
- Added IIS static-download MIME mappings for `.nupkg`, `.sha256`, and `.yxi`
  so the documented NuGet packages, checksums, and direct Alteryx installer are
  downloadable. Verified 24 HTML pages with HTTP GET and all 61 market-table
  download/license targets with HTTP HEAD, with zero failures.

## 2026-08-11

- Added browser-ready HTML copies of the complete function catalog, IRIS
  README and function-output mapping, and Tableau function-output and output-
  schema mappings to the evaluation repository change set.
- Relocated the evaluation installation guides to
  `SAVEDFILES/DataAIETLevaluation/InstallationGuide/` and the production
  installation guides to
  `SAVEDFILES/DataAIETLproduction/InstallationGuide/`. Replaced the combined
  root index with a separate index in each distribution folder, corrected the
  22 market-table guide links and every guide-relative market, index, package,
  and license link, and removed the obsolete root `InstallationGuide/` folder.
  Verified 25 HTML pages with HTTP GET and 86 unique internal links with HTTP
  HEAD, with zero failures.
- Redirected all ETL artifact, checksum, inventory, report, and agreement
  downloads through a path-safe ASP.NET endpoint backed by
  `C:\uploads\SAVEDFILES\DataAIETLevaluation` and
  `C:\uploads\SAVEDFILES\DataAIETLproduction`. License, installation-guide,
  index, and release-information HTML pages remain in their existing
  DataAISandbox locations. The endpoint restricts channels, filenames, and
  file extensions and passed traversal, invalid-channel, blocked-extension,
  and missing-file tests.
- Closed the installation-page audit gaps by adding the Java 17 / Spark 3.5.0
  / Scala 2.12 runtime baseline, explicit production-candidate and marketplace
  status, commercial authorization and fulfillment steps, a sample-agreement
  download, per-row production checksum links, and Spark companion/checksum
  links for evaluation IRIS, Power BI, and Tableau. Verified all 12 uploaded
  evaluation and 13 uploaded production artifact hashes, 48 HTML pages, and 42
  unique upload-backed download URLs with zero failures.
